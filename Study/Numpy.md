"""  PYTHON LIBRARIES """

1) NUMPY:
    1. Numpy is a python library to perform different array operations on data to get bettet insights.
    2. It works similar to List and the only difference is that we can pick all the data at once(List picks one at aa time).
    3. It uses 'C' code at backend to perform the vector operations that we perform on array.
    4. Initailly to use Numpy we have to import the python NUMPY Library.
    5. Append always works at the end extend particular ti stretch element 
    6. table.desc to describe the data show databases t get the list of databases. use dbname to use the particular databse.
    7.  In and not In clause generally used with the subquery.(exist not exist).
    8. Isnull() returns the data which is null. generally used for data cleaning and transformation.
    9.savepoint in DTL, update with index and 'and' reddish mongodb 
    cascading goes witg delete update foriegn key ca be nul also , it can be duplicate 